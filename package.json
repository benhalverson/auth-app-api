{
	"name": "auth-app-api",
	"type": "module",
	"scripts": {
		"dev": "wrangler dev",
		"deploy": "wrangler deploy --minify",
		"cf-typegen": "wrangler types --env-interface CloudflareBindings",
		"auth:generate": "npx @better-auth/cli@latest generate --config ./auth.ts --output ./src/db/auth-schema.ts --yes"
	},
	"dependencies": {
		"better-auth": "^1.3.32",
		"better-sqlite3": "^12.4.1",
		"drizzle-orm": "^0.44.7",
		"hono": "^4.10.3"
	},
	"devDependencies": {
		"drizzle-kit": "^0.31.5",
		"wrangler": "^4.45.0"
	}
}